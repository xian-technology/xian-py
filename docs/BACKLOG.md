# Backlog

This is the stable backlog entrypoint for `xian-py`.

Primary deeper note:

- `SDK_REVIEW_BACKLOG.md`

Current themes:

- transport ergonomics
- typed SDK surfaces
- keeping the SDK aligned with the stack’s newer query and operator endpoints

